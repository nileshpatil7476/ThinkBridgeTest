import { DataNode } from "node_modules_old/domhandler/lib";

export class Inventory {
    id:number= 0;
    name: string='';
    description: string='';
    price: number =0;
    quantity: number=0;
    expiryDate : string|null= '';
}
